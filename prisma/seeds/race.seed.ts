import { container } from "../../src/core/container/";
import { RaceCreateDTO } from "../../src/modules/race/application/dtos/RaceCreateDTO";

const items: RaceCreateDTO[] = [];

export const seedRace = async () => {
  items.forEach(async (item) => {});
};
